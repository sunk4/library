import * as React from 'react'
import Wrapper from "./Wrapper"

interface IAppProps {}

const Students: React.FunctionComponent<IAppProps> = (props) => {
  return <Wrapper>Students</Wrapper>
}

export default Students
