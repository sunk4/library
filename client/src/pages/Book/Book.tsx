import * as React from 'react';
import Wrapper from './Wrapper';

interface IAppProps {
}

const Book: React.FunctionComponent<IAppProps> = (props) => {
  return <Wrapper>HEll</Wrapper>
}

export default Book
